{
  "name": "web",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "firebase-tools": "^8.10.0"
  },
  "devDependencies": {},
  "scripts": {
    "ng": "ng",
    "start": "npm serve -o",
    "build": "npm build --prod",
    "test": "npm test",
    "lint": "npm lint",
    "e2e": "npm e2e",
    "install:clean": "rm -rf node_modules/ && rm -rf package-lock.json && npm install",
    "deploy": "node node_modules/firebase-tools/lib/bin/firebase deploy --only hosting:web-e2l",
    "deploy:bitbucket": "node node_modules/firebase-tools/lib/bin/firebase deploy --only hosting:web-e2l --token $FIREBASE_TOKEN"
  }
}
